
version: 2

sources:
    - name: bronze
      tables:
        - name: posts

models:
    - name: clean_posts
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null


